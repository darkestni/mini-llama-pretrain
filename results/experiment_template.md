# 实验记录

> 训练完成后填写本文件。

## 基本信息
- 实验日期：2026-__-__
- 模型：MiniLlama（缩小版 Llama 架构，~111M 参数）
- 架构：6 层 × 576 维 × 9 头
- 数据：Wikipedia 中文子集（__ 篇文章 / __ tokens）
- 训练步数：____
- GPU：____
- 训练时长：____ 小时

## 核心结果：随机初始 vs 预训练后

| 指标 | 随机初始 | 预训练后 | 变化 |
|------|---------|---------|------|
| Loss | ~12 | __ | ↓ |
| Perplexity | ~30000 | __ | ↓ |
| 生成质量 | 纯乱码 | __ | — |

## 训练曲线
> Loss 随训练步数的下降曲线：results/training_loss.png

观察：
- Loss 从初始 ~__ 降到最终 __
- 在第 __ 步后趋于收敛

## 生成对比（case study）

### 随机初始（训前）
- prompt: "机器学习"
- 生成: __（应是乱码/无意义 token）

### 预训练后
- prompt: "机器学习"
- 生成: __（应出现连贯的、与机器学习相关的文本）

## 关键结论（面试话术要点）

1. **从零预训练**：从随机权重训了 111M 参数的 Llama 架构模型，完成了真正的预训练流程（不是微调）。
2. **数据与目标**：用 Wikipedia 无标注文本做因果语言建模（CLM），loss 从 ~12 降到 __，困惑度从 ~30000 降到 __。
3. **架构可迁移**：用的是标准 LlamaForCausalLM，只是缩小到 111M，架构与 Llama/Qwen 一致，经验可直接迁移到大模型。
4. **工程能力**：完整覆盖「数据 tokenize+打包 → 随机初始化 → CLM 训练 → 困惑度评测 → 生成对比」全链路。
